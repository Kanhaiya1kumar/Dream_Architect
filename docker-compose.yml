version: '3.9'
services:
  api:
    build: ./backend
    ports: ["8000:8000"]
  web:
    build: ./frontend
    environment:
      - NEXT_PUBLIC_API_URL=http://api:8000
    ports: ["3000:3000"]
    depends_on: [api]
